import React from 'react'

export default function ServerIcon({ image }) {
  return (
 <img src={image} alt="" className='server__img object-cover rounded-full cursor-pointer transition-all duration-100 ease-out hover:rounded-2xl' />   
  )
}
